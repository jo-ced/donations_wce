---
interface Props {
  lines: number[];
}

const { lines } = Astro.props;
---

<div class="flex flex-col gap-[7px]">
  {lines.map((w) => (
    <div style={`width: ${w}%; height: 9px; border-radius: 4px; background: var(--color-bar);`}></div>
  ))}
</div>
