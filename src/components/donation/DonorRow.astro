---
import Avatar from '../Avatar.astro';

interface Props {
  name: string;
  amount: number;
  currency: string;
  timeAgo: string;
}

const { name, amount, currency, timeAgo } = Astro.props;
---

<div class="flex items-center gap-2.5 py-2">
  <Avatar size={30} name={name} />
  <div class="flex-1">
    <span class="text-[13px] text-text block">{name}</span>
    <span class="text-[12px] text-text-light block mt-0.5">{timeAgo}</span>
  </div>
  <span class="text-[13px] font-semibold text-text-mid">{currency}{amount}</span>
</div>
